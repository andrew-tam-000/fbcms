import React from 'react';

const Homepage = ({content, date, title}) => (
    <div>
        <div>
            { content }
        </div>
        <div>
            { date }
        </div>
        <div>
            { title }
        </div>
        hello
    </div>
);

export default Homepage;
